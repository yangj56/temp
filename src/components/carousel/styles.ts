import styled from 'styled-components';
import tw from 'twin.macro';

export const ImageWrapper = styled.img`
  ${tw`bg-indigo-500 flex flex-wrap items-center justify-between w-full h-80 p-5 object-contain`};
`;
