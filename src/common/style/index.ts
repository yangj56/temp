import styled from 'styled-components';
import tw from 'twin.macro';

export const LayoutDiv = styled.div`
  ${tw`w-full lg:w-large md:w-mid mx-auto`};
`;

export const SecondaryColorBg = styled.div`
  ${tw`bg-red-500`};
`;
